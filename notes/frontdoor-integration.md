# Integrating with the Trainer Agent — moved

> **This doc has moved.** The canonical, copy-able interface spec is now
> [`INTERFACE.md`](../INTERFACE.md) at the repo root.

`INTERFACE.md` is the single file to copy into a consumer repo. It defines three
interfaces at once — **conceptual** (what the Trainer Agent is for),
**collaboration** (how to file development requests in Linear), and **technical**
(the HTTP wire contract that used to live here). It carries the interface version
and changelog.

If you copied this file into another repo, replace it with `INTERFACE.md`.
